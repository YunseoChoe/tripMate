import React from "react";
import ReactDOM from "react-dom/client";
import Description_mytrip from "../trip_list.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Description_mytrip />
  </React.StrictMode>
);